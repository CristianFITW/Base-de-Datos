<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Carrito de Compras</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      background: url('/img_fondo/f7.jpg') no-repeat center center fixed;
      background-size: cover;
      font-family: 'Segoe UI', sans-serif;
      color: white;
    }

    body::before {
      content: "";
      position: fixed;
      top: 0; left: 0;
      width: 100%; height: 100%;
      background-color: rgba(0, 0, 0, 0.6);
      backdrop-filter: blur(5px);
      z-index: -1;
    }

    .container {
      max-width: 700px;
      margin: 6% auto;
      padding: 30px;
      background-color: rgba(255, 255, 255, 0.07);
      border-radius: 20px;
      box-shadow: 0 0 20px rgba(255, 255, 255, 0.1);
    }

    h1 {
      text-align: center;
      margin-bottom: 25px;
    }

    .empty {
      text-align: center;
      font-size: 1.1rem;
      padding: 20px;
      background-color: rgba(255, 255, 255, 0.05);
      border-radius: 12px;
    }

    ul {
      list-style: none;
      padding: 0;
    }

    li {
      background-color: rgba(255, 255, 255, 0.08);
      margin: 10px 0;
      padding: 15px;
      border-radius: 12px;
      box-shadow: 0 0 10px rgba(255, 255, 255, 0.05);
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .total {
      text-align: right;
      font-weight: bold;
      font-size: 1.2rem;
      margin-top: 20px;
    }

    .btn-pagar {
      display: block;
      margin: 30px auto 0;
      padding: 12px 25px;
      background-color: rgba(255, 255, 255, 0.2);
      border: none;
      border-radius: 12px;
      color: white;
      font-weight: bold;
      cursor: pointer;
      transition: background 0.3s ease;
      text-decoration: none;
      text-align: center;
    }

    .btn-pagar:hover {
      background-color: rgba(255, 255, 255, 0.4);
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Tu carrito</h1>

    <% if (items.length === 0) { %>
      <p class="empty">Tu carrito está vacío 🛒</p>
    <% } else { %>
      <ul>
        <% items.forEach(item => { %>
          <li>
            <div>
              <strong><%= item.Titulo %></strong><br />
              <small><%= item.Artista %></small>
            </div>
            <span>$<%= item.Precio.toFixed(2) %></span>
          </li>
        <% }) %>
      </ul>

      <p class="total">Total: $<%= total.toFixed(2) %></p>

      <a class="btn-pagar" href="/carrito/pagar">Pagar</a>
    <% } %>
  </div>
</body>
</html>
